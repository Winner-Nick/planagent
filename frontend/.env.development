# Frontend dev defaults: talk to the FastAPI bridge on localhost.
#
# Set VITE_USE_FIXTURES=1 to switch back to offline fixture data (no backend
# required) — useful for UI work in a train / plane / pair-programming
# session where the WeChat bot isn't available.
VITE_API_BASE=http://localhost:8000
VITE_USE_FIXTURES=0
